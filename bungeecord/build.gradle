dependencies {
    api project(':common')

    api group: 'net.md-5', name: 'bungeecord-api', version: project.ext.bungeecord
    api group: 'com.github.Bastian.bStats-Metrics', name: 'bstats-bungeecord', version: project.ext.bstats
}

shadowJar {
    archiveClassifier = null

    dependencies {
        include(dependency('com.alessiodp.core.bungeecord:.*'))
        include(dependency('com.github.Bastian.bStats-Metrics:bstats-bungeecord'))
    }

    relocate("org.bstats", "com.alessiodp.core.bungeecord.addons.external.bstats")
}